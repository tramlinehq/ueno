name: iOS Fastlane AdHoc

on:
  workflow_dispatch:
    inputs:
      versionName:
        description: "User-facing release version name"
        required: true
        default: "1.0.0"
      versionCode:
        description: "versionCode or build number"
        required: true
        default: "1"

jobs:
  deploy:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup SSH Agent
        uses: webfactory/ssh-agent@v0.7.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Create and setup keychain for CI
        run: |
          security create-keychain -p temp_password fastlane_tmp.keychain
          security set-keychain-settings -t 3600 -u fastlane_tmp.keychain
          security default-keychain -s fastlane_tmp.keychain
          security unlock-keychain -p temp_password fastlane_tmp.keychain

      - name: Set up ruby env
        uses: ruby/setup-ruby@v1.171.0
        with:
          ruby-version: 3.2.1
          bundler-cache: true

      - name: Setup the flutter environment
        uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.22.0"
          channel: "stable"
          cache: true

      - name: Get flutter dependencies
        run: flutter pub get

      - name: Build the app
        run: |
          flutter build ipa --debug --build-number=${{ github.event.inputs.versionCode }} --build-name=${{ github.event.inputs.versionName }} --no-codesign --dart-define=BUGSNAG_API_KEY=${{ secrets.BUGSNAG_API_KEY }}
          ls -la build/ios/archive

      - name: Generate and sign the ipa
        working-directory: ios
        run: |
          echo "MATCH_PASSWORD is set: $([ -n "$MATCH_PASSWORD" ] && echo "YES" || echo "NO")"
          echo "MATCH_PASSWORD length: ${#MATCH_PASSWORD}"
          echo "$MATCH_PASSWORD" | od -c | head -1

          export MATCH_PASSWORD="sixthdeputy"

          echo "MATCH_PASSWORD is set: $([ -n "$MATCH_PASSWORD" ] && echo "YES" || echo "NO")"
          echo "MATCH_PASSWORD length: ${#MATCH_PASSWORD}"
          echo "$MATCH_PASSWORD" | od -c | head -1

          bundle install
          bundle exec fastlane match adhoc --readonly --verbose
          bundle exec fastlane ios ad_hoc_build
        env:
          ASC_KEY_ID: ${{ secrets.APPSTORE_API_KEY_ID }}
          ASC_ISSUER_ID: ${{ secrets.APPSTORE_ISSUER_ID }}
          ASC_KEY: ${{ secrets.APPSTORE_API_PRIVATE_KEY }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          MATCH_KEYCHAIN_NAME: fastlane_tmp.keychain
          MATCH_KEYCHAIN_PASSWORD: temp_password

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: iOS IPA
          path: ios/*.ipa
