git_url("git@github.com:tramlinehq/ueno-certs.git")
storage_mode("git")
type(ENV["MATCH_TYPE"] || "development")
app_identifier(["com.tramline.ueno"])
team_id("6XCS8KZXDA")
git_branch("main")
keychain_name(ENV["MATCH_KEYCHAIN_NAME"] || "fastlane_tmp")
keychain_password(ENV["MATCH_KEYCHAIN_PASSWORD"] || "temp_password")
